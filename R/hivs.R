# install the devtools package, which is required to install
# the HIEv package if necessary
install.packages("devtools")

#install the HIEv package, and then load it so that it can be used
devtools::install_bitbucket("remkoduursma/HIEv")

